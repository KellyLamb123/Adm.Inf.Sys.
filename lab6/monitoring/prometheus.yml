global:
  scrape_interval: 5s
  evaluation_interval: 5s

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:
  - job_name: nginx

    static_configs:
      - targets:
          - nginx-exporter:9113